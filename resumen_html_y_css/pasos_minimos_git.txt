Los pasos se siguen en este orden

git init

git add .

git commit -m "Etiquetas HTML básicas"

$ git remote add origin https://github.com/classesdeferran/resumen-html-y-css.git
git branch -M main
git push -u origin main
